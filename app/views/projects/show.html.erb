<% @title=param_user + '/' + @space.name + '/' + @project.name %>
<div class="row">
		<div class="panel panel-success">
			<div class="panel-body">
        <h3>Edit project</h3>
        <br />
				<div class="panel panel-success">
					<div class="panel-body">
						<%= render 'edit_project' %>
					</div>
				</div>

				<br />
				<h3>Configuration</h3>
				<div class="panel panel-success">
					<div class="panel-body">
						<%= render 'config_project' %>
					</div>
				</div>
    	</div>
  </div>
</div>
